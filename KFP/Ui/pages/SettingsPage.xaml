<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KFP.Ui.pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:components="using:KFP.Ui.Components">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="40,40,0,0">
        <TextBlock FontSize="22" x:Uid="GeneralSettings"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="20,25,0,0">
            <TextBlock x:Uid="App_Window_Display" FontSize="18"/>
            <RadioButtons Margin="10,10,0,0">
                <RadioButton x:Name="FullScreenRadio" x:Uid="FullScreen" Checked="RadioButtonFullScreen_Checked"/>
                <RadioButton x:Name="OverlappedRadio" x:Uid="Overlapped" Checked="RadioButton_CheckedOverlapped"/>
            </RadioButtons>
        </StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="20,25,0,0">
            <TextBlock x:Uid="Currency" FontSize="18"/>
            <ComboBox x:Name="CurrencyComboBox"
                      ItemsSource="{x:Bind Currencies, Mode=OneWay}"
                      SelectedItem="{x:Bind Currency, Mode=TwoWay}"
                      Width="200"
                      Margin="10"/>
        </StackPanel>
        <TextBlock FontSize="22" x:Uid="RestaurantTextBox" Margin="0,20,0,0"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="20,25,0,0">
            <TextBlock x:Uid="RestaurantNameTextBox" FontSize="18"/>
            <TextBox x:Name="RestaurantNameBox"
               Text="{x:Bind RestaurantName, Mode=TwoWay}" 
               Width="200"
               Margin="10"/>
        </StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="20,25,0,0">
            <TextBlock x:Uid="NumberOfTablesTextBlock" FontSize="18"/>
            <components:KioberNumberBox x:Name="NumberOfTablesBox"
                       Value="{x:Bind NumberOfTables, Mode=TwoWay}" AllowOnlyIntgers="True" AllowOnlyPositiveValues="True" MaxValue="99"
                       Width="200"
                       Margin="10"/>
        </StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="20,25,0,0">
            <TextBlock x:Uid="orderLateDelayTextBlock" FontSize="18"/>
            <StackPanel Orientation="Horizontal">
                <components:KioberNumberBox x:Name="orderLateInput"
                    Value="{x:Bind OrderLateDelay, Mode=TwoWay}" AllowOnlyIntgers="True" AllowOnlyPositiveValues="True" MaxValue="120"
                    Width="200"
                    Margin="10,10,30,10"/>
                <TextBlock x:Uid="minutesTextBlock" FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock x:Uid="orderOverdueDelayTextBlock" FontSize="18"/>
            <StackPanel Orientation="Horizontal">
                <components:KioberNumberBox x:Name="orderOverDueInput"
                                            Value="{x:Bind OrderOverDueDelay, Mode=TwoWay}" AllowOnlyIntgers="True" AllowOnlyPositiveValues="True" MaxValue="120"
                                            Width="200"
                                            Margin="10,10,30,10"/>
                <TextBlock x:Uid="minutesTextBlock" FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>

</Page>
