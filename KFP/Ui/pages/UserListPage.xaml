<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KFP.Ui.pages.UserListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:converters="using:KFP.Converters">

    <Page.Resources>
        <converters:RoleConverter x:Key="roleConverter"/>
        <converters:AvatarCodeToImageSourceConverter x:Key="avatarConverter"/>
        <converters:avatarConnectedConverter x:Key="avatarConected"/>
    </Page.Resources>

    <Grid x:Name="PageContent" Margin="80,40,20, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Stretch" Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>

            <TextBlock Margin="25,0,0,0" x:Uid="AvatarTextBlock" Grid.Column="0"  FontSize="20"  HorizontalAlignment="Center"/>
            <TextBlock x:Uid="UsernameTextBlock" Grid.Column="1"  FontSize="20" HorizontalAlignment="Center"/>
            <TextBlock x:Uid="UserRoleTextBlock" Grid.Column="2"  FontSize="20" HorizontalAlignment="Center"/>
            <TextBlock Margin="0,0,30,0" x:Uid="ActionsTextBlock"  Grid.Column="3"  HorizontalAlignment="Center" FontSize="20"/>
        </Grid>
        <ListView ItemsSource="{x:Bind UserListElements}"  Grid.Row="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollMode="Auto">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:UserListElement">
                    <Grid HorizontalAlignment="Stretch" Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="80"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <!--max CornerRadius for round image-->
                        <Border CornerRadius="1000" Grid.Column="0" BorderThickness="{x:Bind IsConnected, Converter={StaticResource avatarConected}}" BorderBrush="{x:Bind IsConnected, Converter={StaticResource avatarConected}}">
                            <Image Source="{x:Bind User.avatarCode, Converter={StaticResource avatarConverter}}"  Width="80" Height="80"/>
                        </Border>
                        <TextBlock Text="{x:Bind User.UserName}" Grid.Column="1" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock Text="{x:Bind User.Role, Converter={StaticResource roleConverter}}"
                               Grid.Column="2" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <CommandBar Grid.Column="3" IsOpen="False" DefaultLabelPosition="Right"  VerticalAlignment="Center" HorizontalAlignment="Center">
                            <AppBarButton Icon="Edit" Command="{Binding GoToEditUserCommand}"/>
                            <AppBarButton Icon="Delete" Command="{Binding DeleteUserCommand}"/>
                        </CommandBar>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
