<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="KFP.Ui.pages.OrderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.pages.Order"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <Grid Margin="0,10,0,10" HorizontalAlignment="Center" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding AddQuantityCommand}" Grid.Column="0" MinWidth="80" Margin="0,0,0,0">
                <FontIcon Glyph="&#xE710;" FontSize="17"/>
            </Button>
            <Button Command="{Binding DeleteOrderItemCommand}" Grid.Column="1" MinWidth="100" Margin="0,0,0,0">
                <FontIcon Glyph="&#xE74D;"  FontSize="17"/>
            </Button>
            <Button Command="{Binding ReduceQuantityCommand}" Grid.Column="2" MinWidth="80" Margin="0,0,0,0">
                <FontIcon Glyph="&#xE73C;" FontSize="17" />
            </Button>
        </Grid>
        <Border Background="#FFF4F4F4" CornerRadius="2" 
    BorderThickness="0" BorderBrush="{ThemeResource SystemAccentColor}" 
    MinHeight="400" Margin="0,0,0,0" HorizontalAlignment="Center" MinWidth="400">
            <ListView ItemsSource="{Binding CurrentOrder.OrderItems}" SelectedItem="{Binding SelectedOrderItem, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding MenuItemName}" />
                            <TextBlock Text="Qty: {Binding Quantity}, Total: {Binding Amount}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </StackPanel>
</UserControl>
