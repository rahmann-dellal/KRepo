<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KFP.Ui.pages.DisplayMenuItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    
    xmlns:converters="using:KFP.Converters" 
    xmlns:model="using:KFP.DATA"
    xmlns:custom="using:KFP.Ui.Components">

    <Page.Resources>
        <converters:MenuItemTypeConverter x:Key="menuItemTypeConverter"/>
    </Page.Resources>
    <Grid Margin="100,50,30,30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="0">
            <TextBlock x:Uid="Items_name" FontSize="16"/>
            <TextBlock Text="{x:Bind _menuItem.ItemName}" Margin="15,10,0,0" FontSize="20"/>
            <TextBlock x:Uid="Items_price" FontSize="16" Margin="0,20,0,0" />
            <RelativePanel>
                <TextBlock Text="{x:Bind _menuItem.SalePrice}" Margin="15,10,0,0" FontSize="20" x:Name="txtblk"/>
                <TextBlock Text="{x:Bind _currency}" RelativePanel.AlignBottomWithPanel="True" RelativePanel.RightOf="txtblk"  Margin="20,15,0,5"/>
            </RelativePanel>
            <TextBlock x:Uid="Item_Type_text_block" FontSize="16" Margin="0,20,0,0" />
            <TextBlock Text="{x:Bind _menuItem.MenuItemType, Converter={StaticResource menuItemTypeConverter}}" Margin="15,10,0,0" FontSize="20"/>
            <TextBlock x:Uid="AssignedCategories"  Margin="0,30,0,10"/>
            <Border MaxHeight="200" MinHeight="100" Margin="15,10,0,30" Width="350" HorizontalAlignment="Left" BorderBrush="LightGray" BorderThickness="2" CornerRadius="5">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock x:Uid="Empty" Foreground="Gray" HorizontalAlignment="Center" Margin="0,10,0,0"
                                   x:Name="EmptyTextBlock" Visibility="Collapsed"/>
                        <ListView ItemsSource="{x:Bind _menuItem.Categories}"  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="model:Category">
                                    <TextBlock Text="{x:Bind CategoryName}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="0">
            <TextBlock Text="Picture" x:Uid="picture_text_block"  HorizontalAlignment="Center" FontWeight="Bold"/>
            <Border CornerRadius="4" BorderThickness="2" BorderBrush="LightGray" Width="300" Height="300" Margin="15,20,0,0">
                <Image x:Name="picture" HorizontalAlignment="Center" Width="300" Height="300" Stretch="UniformToFill"/>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,0,0,0">
            <Button  Width="200" HorizontalContentAlignment="Center" Command="{x:Bind GoToEditPageCommand}"
    Foreground="{ThemeResource TextOnAccentFillColorPrimary}" Background="{ThemeResource AccentFillColorDefaultBrush}" BorderBrush="{ThemeResource ProgressBarBorderThemeBrush}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE70F;" />
                    <TextBlock x:Uid="Edit_Menu_Item_Button" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
            <Button  Width="200" HorizontalContentAlignment="Center" Command="{x:Bind DeleteCommand}"
        Margin="20,0,0,0" 
        Foreground="{ThemeResource SystemFillColorCaution}" BorderBrush="{ThemeResource SystemFillColorCaution}" Background="{ThemeResource SystemFillColorCautionBackground}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE74D;" />
                    <TextBlock x:Uid="Delete_Menu_Item_Button" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
