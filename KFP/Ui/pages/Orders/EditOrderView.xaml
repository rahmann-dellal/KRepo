<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="KFP.Ui.pages.EditOrderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:models="using:KFP.DATA"
    xmlns:vm="using:KFP.ViewModels"
    mc:Ignorable="d" Loaded="OrderView_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,10,0,10" HorizontalAlignment="Center" Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Command="{x:Bind ViewModel.AddQuantityCommand}" Grid.Column="0" MinWidth="80" Margin="0,0,0,0">
                <FontIcon Glyph="&#xE710;" FontSize="24"/>
            </Button>
            <Button Command="{x:Bind ViewModel.RemoveOrderItemCommand}" Grid.Column="1" MinWidth="100" Margin="0,0,0,0">
                <FontIcon Glyph="&#xE74D;"  FontSize="24"/>
            </Button>
            <Button Command="{x:Bind ViewModel.ReduceQuantityCommand}" Grid.Column="2" MinWidth="80" Margin="0,0,0,0">
                <FontIcon Glyph="&#xE738;" FontSize="24" />
            </Button>
        </Grid>
        <Border Background="{StaticResource SecondBackgroud}" CornerRadius="10" Padding="10,10,0,10" Grid.Row="1" 
                BorderThickness="0" BorderBrush="{ThemeResource SystemAccentColor}" 
                 Margin="0,0,0,0" HorizontalAlignment="Center" MinWidth="650" MinHeight="200">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="200"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="45"/>

                    </Grid.ColumnDefinitions>
                    <TextBlock x:Uid="MenuItemTextBlock1" FontSize="16" FontWeight="SemiBold"  Grid.Column="0" Margin="10,0,0,0"/>
                    <TextBlock x:Uid="UnitPriceTextBlock"  FontSize="16" FontWeight="SemiBold"  Grid.Column="1" Margin="-15,0,0,0"/>
                    <TextBlock x:Uid="QtTextBlock" FontSize="16" FontWeight="SemiBold"  Grid.Column="2" Margin="-15,0,0,0"/>
                    <TextBlock x:Uid="TotalTextBlock" FontSize="16" FontWeight="SemiBold"  Grid.Column="3" Margin="-15,0,0,0"/>
                </Grid>
                <StackPanel Grid.Row="1" Margin="0,20,0,20"
                            VerticalAlignment="Center" HorizontalAlignment="Center" 
                            Visibility="{x:Bind ViewModel.isListEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock x:Uid="NothingTextBlock" Margin="0,0,0,10" Foreground="DimGray"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Uid="SelectMenuItemTextBlock"  FontSize="16" Foreground="DimGray" Margin="0,0,10,0" />
                        <FontIcon Glyph="&#xE72A;" Foreground="DimGray" FontSize="14" Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                <TreeView x:Name="OrderTreeView" ItemsSource="{x:Bind ViewModel.OrderItemElements, Mode=OneWay}" Grid.Row="1" Margin="0,10,0,0"
                                       SelectedItem="{x:Bind ViewModel.selectedOrderItemElement, Mode=TwoWay}"
                                       CanDragItems="False" CanDrag="False" AllowDrop="False">
                    <TreeView.ItemTemplate>
                        <DataTemplate x:DataType="vm:OrderItemListElement">
                            <TreeViewItem Grid.Column="0"
                                           IsExpanded="True"
                                           ItemsSource="{x:Bind AddOnItemElements, Mode=TwoWay}" CanDrag="False" AllowDrop="False">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="200"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0" >
                                        <TextBlock Text="+" Margin="0,0,5 ,0" Visibility="{x:Bind IsAddOn, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Text="{x:Bind orderItem.MenuItemName}" FontSize="16" />
                                    </StackPanel>
                                    
                                    <TextBlock Text="{x:Bind orderItem.UnitPrice}" FontSize="16"   Grid.Column="1" Grid.Row="0" Margin="15,0,0,0"/>
                                    <TextBlock Text="{x:Bind orderItem.Quantity, Mode=OneWay}" FontSize="16"   Grid.Column="2" Grid.Row="0" Margin="15,0,0,0"/>
                                    <TextBlock Text="{x:Bind orderItem.TotalPrice, Mode=OneWay, Converter={StaticResource DoubleToStringConverter}, ConverterParameter='F2'}" FontSize="16" Grid.Column="3" Grid.Row="0" Margin="15,0,0,0"/>
                                    <Button  Command="{x:Bind selectToAddAddonsCommand}"  Grid.Column="4" Grid.Row="0" Margin="5,0,0,0"
                                                Visibility="{x:Bind canSelectToAddAddons, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                                        <PathIcon Data="{StaticResource  AddAddonIcon}"/>
                                    </Button>
                                    <Button Command="{x:Bind unselectToAddAddonsCommand}" Grid.Column="4" Grid.Row="0" Margin="5,0,0,0"
                                                Visibility="{x:Bind isAddingAddons, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                                        <PathIcon Data="{StaticResource RemoveAddonIcon}"/>
                                    </Button>
                                    <TextBlock x:Uid="SelectAddOnsTxtBlock" Foreground="Green" Margin="10,0,0,0" Grid.ColumnSpan="5" Grid.Row="1"
                                                   Visibility="{x:Bind isAddingAddons, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </Grid>
                            </TreeViewItem>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
        </Border>
        <Grid Grid.Row="2" Margin="10" Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Left" Command="{x:Bind ViewModel.ClearOrderCommand}" >
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Uid="ClearOrderTextBlock" FontSize="16" FontWeight="SemiBold" Margin="0,0,10,0"/>
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                </StackPanel>
            </Button>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Uid="TotalPriceTxtBlock" FontSize="18"  Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{x:Bind ViewModel.orderTotalPrice,Mode=OneWay, Converter={StaticResource DoubleToStringConverter}}" FontSize="22" FontWeight="SemiBold" Margin="0,0,10,0" />
                <TextBlock Text="{x:Bind ViewModel.Currency, Mode=OneWay}" FontSize="18"  VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
