<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="KFP.Ui.Components.Pager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:vm="using:KFP.ViewModels"
    mc:Ignorable="d">

    <Grid  VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <!-- Previous -->
        <Button Content="⟨" Grid.Column="0"
                Command="{x:Bind PreviousPageCommand}" />

        <!-- Page Buttons -->
        <ItemsControl x:Name="PagesRepeater" ItemsSource="{x:Bind PageCommands, Mode=OneWay}" Grid.Column="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <controls:WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:PageCommand">
                    <Border BorderBrush="{x:Bind IsSelected, Converter={StaticResource BoolToBrushConverter}, Mode=OneWay}"
                            BorderThickness="{x:Bind IsSelected, Converter={StaticResource BoolToBrushConverter},ConverterParameter=2, Mode=OneWay}"
                            CornerRadius="6"
                            Margin="2">
                        <Button Command="{x:Bind Command}"
                                Padding="10,4"
                                MinWidth="32"
                                CornerRadius="6">
                            <TextBlock Text="{x:Bind PageNumber}" />
                        </Button>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Next -->
        <Button Content="⟩" Grid.Column="2"
                Command="{x:Bind NextPageCommand}" />

    </Grid>
</UserControl>
