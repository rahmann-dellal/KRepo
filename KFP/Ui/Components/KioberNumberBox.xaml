<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="KFP.Ui.Components.KioberNumberBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KFP.Ui.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="NumberTextBox" Grid.Row="0" Grid.Column="0"
                 Grid.RowSpan="2" VerticalAlignment="Center"
                 HorizontalAlignment="Center" TextAlignment="Center"
                 Margin="20,0,12,0" Width="90"
                 FontSize="20"/>

        <Button x:Name="UpButton" Grid.Row="0" Grid.Column="1" Content="▲" VerticalAlignment="Center" Click="OnUpButtonClick" FontSize="14"  Padding="2,0,2,0"/>
        <Button x:Name="DownButton" Grid.Row="1" Grid.Column="1" Content="▼" VerticalAlignment="Center" Click="OnDownButtonClick" FontSize="14" Padding="2,0,2,0"/>
    </Grid>
</UserControl>
